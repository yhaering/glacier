{
  "private": true,
  "scripts": {
    "workspace:ci": "turbo run ci",
    "workspace:clean": "git clean -Xdf",
    "prepare": "husky install"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.8.1",
    "@commitlint/config-conventional": "^18.6.2",
    "husky": "^8.0.3",
    "turbo": "^1.12.2"
  },
  "workspaces": [
    "packages/development/*",
    "packages/runtime/*"
  ]
}
