import type { ParserContext } from '../../interfaces/ParserContext';
import type { DecimalDigitsNode } from './interfaces/DecimalDigitsNode';

export function parseDecimalDigits(context: ParserContext): DecimalDigitsNode | undefined {
  return;
}
