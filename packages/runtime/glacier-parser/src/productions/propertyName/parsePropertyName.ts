import type { ParserContext } from '../../interfaces/ParserContext';
import type { PropertyNameNode } from './interfaces/PropertyNameNode';

export function parsePropertyName(context: ParserContext): PropertyNameNode | undefined {
  return;
}
