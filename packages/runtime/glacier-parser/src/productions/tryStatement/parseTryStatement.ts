import type { ParserContext } from '../../interfaces/ParserContext';
import type { TryStatementNode } from './interfaces/TryStatementNode';

export function parseTryStatement(context: ParserContext): TryStatementNode | undefined {
  return;
}
