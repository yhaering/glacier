import type { ParserContext } from '../../interfaces/ParserContext';
import type { IdentifierNode } from './interfaces/IdentifierNode';

export function parseIdentifier(context: ParserContext): IdentifierNode | undefined {
  return;
}
