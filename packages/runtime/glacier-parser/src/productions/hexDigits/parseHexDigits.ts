import type { ParserContext } from '../../interfaces/ParserContext';
import type { HexDigitsNode } from './interfaces/HexDigitsNode';

export function parseHexDigits(context: ParserContext): HexDigitsNode | undefined {
  return;
}
