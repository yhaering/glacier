import type { ParserContext } from '../../interfaces/ParserContext';
import type { ArgumentListNode } from './interfaces/ArgumentListNode';

export function parseArgumentList(context: ParserContext): ArgumentListNode | undefined {
  return;
}
